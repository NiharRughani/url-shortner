# url-shortner
The URL Shortener is a web-based application that converts long and complex URLs into short, easy-to-share links. This helps users share links more conveniently, Users can input a long URL and the system generates a unique short code for it. When someone accesses the short URL, the application automatically redirects them to the original long URL.
